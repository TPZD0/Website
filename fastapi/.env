OPENAI_API_KEY=""

# Google OAuth (fill in your credentials)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Redirect URIs (adjust for your environment if needed)
# Backend callback where Google redirects
GOOGLE_REDIRECT_URI="http://localhost:8000/api/auth/google/callback"
# Frontend base URL used for post-login redirect
FRONTEND_URL="http://localhost:3000"

# SMTP settings for email verification (fill in for production)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="no-reply@example.com"

# Public backend base URL used in verification links
BACKEND_BASE_URL="http://localhost:8000"
